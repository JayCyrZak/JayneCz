using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Box : MonoBehaviour
{
    public bool _is_inpected;

    // Start is called before the first frame update
    void Start()
    {
        _is_inpected = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
